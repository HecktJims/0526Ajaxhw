@{
    ViewData["Title"] = "Address";
}

<h1>Address</h1>
<select id="selectCity">
    <option value="a">Item1</option>
    <option value="b">Item2</option>
    <option value="c">Item3</option>
    <option value="d">Item4</option>
</select>
<select id="selectDist"></select>
<select id="selectRoad"></select>

@section Styles{
    <style>
    </style>
}
@section Scripts{
    <script>
        const selCity = document.querySelector('#selectCity');
        const selDist = document.querySelector('#selectDist');
        const selRoad = document.querySelector('#selectRoad');

        //ajax
        (async () => {
            const response = await fetch('@Url.Content("~/api/cities")');
            const datas = await response.json();
            const cities = datas.map((city, index) => {
                return `<option value="${index}">${city}</option>`;
            })
            selCity.innerHTML = cities.join("");
        })();

        (async () => {
            const response = await fetch('@Url.Content("~/api/cities")');
            const datas = await response.json();
            const cities = datas.map((city, index) => {
                return `<option value="${index}">${city}</option>`;
            })
            selCity.innerHTML = cities.join("");
        })();

        (async () => {
            const response = await fetch('@Url.Content("~/api/cities")');
            const datas = await response.json();
            const cities = datas.map((city, index) => {
                return `<option value="${index}">${city}</option>`;
            })
            selCity.innerHTML = cities.join("");
        })();


    </script>
}